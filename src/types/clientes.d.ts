
// Este arquivo pode estar vazio ou conter tipos relacionados a clientes
// que não são utilizados atualmente no sistema
export interface Cliente {
  id: string;
  nome: string;
  email?: string;
  telefone?: string;
  created_at: string;
  updated_at: string;
}
